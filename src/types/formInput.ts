export interface formInput {
    label?: string;
    placeholder?: string;
    type?: 'text' | 'password' | 'email' | 'number';
    idName?: string,
    className?: string
    hiddenLabel?: boolean
}
